# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
baseurl: https://fl77n.github.io
url: "" # the base hostname & protocol for your site, e.g. http://example.com
title: From L77
author: L77
email: feizzhang@163.com
description: Personal Blog
github_username:  fl77n
show_excerpts: true

name: tufte
markdown: kramdown
markdown_ext: "markdown,mkdown,mkdn,mkd,md"
excerpt_separator: <!--more-->  # you can specify your own separator, of course.
exclude: ['Gemfile', 'Gemfile.lock', 'Rakefile', 'UploadToGithub.Rakefile', 'README.md']
post:
  template: _post.txt
  extension: md
page:
  template: _page.txt
  extension: md
editor: gvim
git:
  branch: master
transfer:
  command: rsync
  settings: -av
  source: _site/
  destination: ~/Git/clayh53.github.com/

plugins:
  - jekyll-paginate
  - jekyll-toc
paginate: 3         # 每页显示的文章数量
paginate_path: "nav/blog/page:num/"  # 分页页面的路径格式

nav_bar:
  - name: "blog"
    link: "nav/blog/index.html"
  - name: "archive"
    link: "nav/archive.html"
  - name: "about"
    link: "nav/about.html"

kramdown:
  auto_ids: true
  syntax_highlighter: rouge

# 目录生成配置（可选）
toc:
  min_level: 1    # 最小标题级别（h1）
  max_level: 3    # 最大标题级别（h3）
  ordered_list: false # 使用无序列表
  slugify: true   # 自动处理标题ID